[build]
  command = "echo \"window.DOCGENIUS_API_BASE = '${DOCGENIUS_API_BASE}';\" > config.js"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
